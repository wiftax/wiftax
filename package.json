{
  "name": "wiftax",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "validate": "node scripts/validate.mjs",
    "render": "node scripts/render.mjs",
    "check": "npm run validate && npm run render && npm run build",
    "build": "node site/build.mjs"
  },
  "devDependencies": {
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1",
    "js-yaml": "^4.1.0",
    "marked": "^18.0.13"
  }
}
